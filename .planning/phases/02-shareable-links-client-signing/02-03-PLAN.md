---
phase: 02-shareable-links-client-signing
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/ClientSign.tsx
  - src/components/WizardProgress.tsx
  - src/components/ClientReviewStep.tsx
  - src/components/ClientPersonalStep.tsx
  - src/components/ClientEmploymentStep.tsx
  - src/components/ClientEmergencyStep.tsx
  - src/router.tsx
autonomous: true
requirements: [CORE-04, SIGN-01, SIGN-05, AUDIT-02]

must_haves:
  truths:
    - "Client opens /sign/:token and sees a branded multi-step wizard"
    - "Step 1 shows admin-filled agreement terms as formatted document text (not disabled inputs)"
    - "Client can fill personal info, employment, and emergency contact fields across steps 2-4"
    - "Progress bar shows current step and total steps"
    - "Client can navigate forward and backward between completed steps"
    - "Form data persists across browser refresh (localStorage via Zustand persist)"
    - "Invalid or expired token redirects to /sign/expired"
    - "Opening the link records a 'viewed' audit log entry"
    - "Form validation runs on step advance with professional error messages"
  artifacts:
    - path: "src/pages/ClientSign.tsx"
      provides: "Wizard container that fetches agreement by token and orchestrates steps"
      min_lines: 60
    - path: "src/components/WizardProgress.tsx"
      provides: "Step progress bar component"
      min_lines: 15
    - path: "src/components/ClientReviewStep.tsx"
      provides: "Step 1 - read-only agreement terms display"
      min_lines: 40
    - path: "src/components/ClientPersonalStep.tsx"
      provides: "Step 2 - personal info form with RHF + zod"
      min_lines: 50
    - path: "src/components/ClientEmploymentStep.tsx"
      provides: "Step 3 - employment info form with RHF + zod"
      min_lines: 40
    - path: "src/components/ClientEmergencyStep.tsx"
      provides: "Step 4 - emergency contact form with RHF + zod"
      min_lines: 40
  key_links:
    - from: "src/pages/ClientSign.tsx"
      to: "src/lib/clientSigning.ts"
      via: "fetchAgreementByToken on mount, recordClientView on first load"
      pattern: "fetchAgreementByToken|recordClientView"
    - from: "src/pages/ClientSign.tsx"
      to: "src/stores/clientSignStore.ts"
      via: "creates store with token and provides via context"
      pattern: "createClientSignStore|ClientSignStoreContext"
    - from: "src/components/ClientPersonalStep.tsx"
      to: "react-hook-form"
      via: "useForm with zodResolver for validation"
      pattern: "useForm.*zodResolver"
    - from: "src/pages/ClientSign.tsx"
      to: "src/pages/ExpiredPage.tsx"
      via: "navigates to /sign/expired on invalid token"
      pattern: "navigate.*expired"
---

<objective>
Build the client-facing multi-step signing wizard container and form steps 1-4 (Review, Personal Info, Employment, Emergency Contact). Client opens /sign/:token, sees branded wizard with progress bar, reviews admin-filled terms as document text, and fills their personal information across validated form steps.

Purpose: Delivers the core client form experience (SIGN-01, SIGN-05) with mobile-first design, auto-save persistence, and professional validation. This is the highest-touch client interaction -- it must feel polished, warm, and trustworthy.

Output: ClientSign wizard page, WizardProgress bar, and four step components with RHF+zod validation.
</objective>

<execution_context>
@C:/Users/jobaw/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jobaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shareable-links-client-signing/02-RESEARCH.md
@.planning/phases/02-shareable-links-client-signing/02-CONTEXT.md
@.planning/phases/02-shareable-links-client-signing/02-01-SUMMARY.md

@src/stores/clientSignStore.ts
@src/lib/clientSigning.ts
@src/types.ts
@src/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WizardProgress and ClientSign wizard container</name>
  <files>
    src/components/WizardProgress.tsx
    src/pages/ClientSign.tsx
    src/router.tsx
  </files>
  <action>
    1. **Create `src/components/WizardProgress.tsx`:**

       A horizontal step progress bar showing the current step in a 6-step flow (Review, Personal, Employment, Emergency, Sign, Confirm).

       **Props:** `currentStep: number`, `totalSteps: number`, `stepLabels: string[]`

       **Design (mobile-first per SIGN-05):**
       - Horizontal bar with circles for each step connected by lines
       - Completed steps: filled forest-green circle with white checkmark
       - Current step: forest-green circle with white number, slightly larger or with ring
       - Future steps: gray outline circle with gray number
       - Connecting lines: forest-green for completed, gray for future
       - Step labels below each circle (truncate on small screens — show only current label on very small widths)
       - Use Tailwind responsive classes. On mobile (< md), show a simplified "Step 2 of 6: Personal Info" text instead of the full bar.

    2. **Create `src/pages/ClientSign.tsx`:**

       The wizard container page rendered at `/sign/:token`.

       **On mount:**
       - Extract `token` from `useParams()`
       - Call `fetchAgreementByToken(token)` from `../lib/clientSigning`
       - If fetch returns null (expired, revoked, or invalid): navigate to `/sign/expired`
       - If agreement status is already 'signed' or 'completed': navigate to `/sign/${token}/complete`
       - Store agreement data in local state for the review step
       - Call `recordClientView(agreement.id)` once on first load (use a ref to prevent duplicate calls on re-renders)
       - Create the client sign store via `createClientSignStore(token)` and provide it via `ClientSignStoreContext.Provider`
       - Set agreementMeta (id, number) on the store

       **Branding (per user decisions):**
       - Full Triple J branding at top: JJAI text logo in gold, "Triple J Auto Investment LLC" in forest-green, company name prominent
       - White/light background, clean spacing
       - Professional + warm tone throughout

       **Wizard orchestration:**
       - Read current step from the client sign store
       - Render `WizardProgress` at the top
       - Render the appropriate step component based on current step number:
         - Step 0: ClientReviewStep
         - Step 1: ClientPersonalStep
         - Step 2: ClientEmploymentStep
         - Step 3: ClientEmergencyStep
         - Step 4: ClientSignStep (placeholder div for now — Plan 04 creates this)
         - Step 5: Review + Submit (placeholder div for now — Plan 04 creates this)
       - Pass agreement data (for read-only display) and step navigation callbacks to each step

       **Step navigation:**
       - Each step component calls `onNext()` when its form validates
       - `onNext()` increments the step in the store (which auto-persists to localStorage)
       - Each step has a "Back" button (except step 0) that decrements the step
       - Step 0 has no back button; it has "I've Reviewed — Continue" button

       **Loading state:** Show a centered spinner/loading indicator while fetching the agreement. Use Triple J branding colors.

       **Error state:** If fetch fails for a reason other than expired (network error), show a "Something went wrong. Please try again." message with a retry button. Professional, not technical.

    3. **Update `src/router.tsx`:**
       - Replace the `/sign/:token` placeholder with `<ClientSign />`
       - Import ClientSign from `./pages/ClientSign`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` passes
    - Navigating to `/sign/invalid-token` redirects to `/sign/expired`
    - WizardProgress renders step indicators
  </verify>
  <done>ClientSign wizard container fetches agreement by token, records view audit, provides store context, and orchestrates step rendering with progress bar. Invalid/expired tokens redirect to ExpiredPage.</done>
</task>

<task type="auto">
  <name>Task 2: Create client form steps 1-4 (Review, Personal, Employment, Emergency)</name>
  <files>
    src/components/ClientReviewStep.tsx
    src/components/ClientPersonalStep.tsx
    src/components/ClientEmploymentStep.tsx
    src/components/ClientEmergencyStep.tsx
  </files>
  <action>
    1. **Create `src/components/ClientReviewStep.tsx` (Step 1):**

       Displays admin-filled agreement terms as formatted document text (per user decision: "reads like a printed contract, not disabled inputs").

       **Props:** `agreementData: AgreementData`, `onNext: () => void`

       **Layout:**
       - Heading: "Agreement Terms" or "Review Your Agreement"
       - Subtext: "Please review the terms of your vehicle rental agreement below."
       - Display vehicle info (year/make/model, VIN, color, plate), rental term (dates, duration, rate), payment terms (all fees, totals, due dates, methods), and mileage options
       - Format each section with a section heading and key-value pairs rendered as styled paragraphs — NOT as input fields. Use a definition-list-like layout:
         ```
         Vehicle: 2024 Toyota Camry SE
         VIN: 1HGBH41JXMN109186
         Color: Silver
         ```
       - Style: Serif font (Lora) for document feel, good line spacing, section dividers
       - "I've Reviewed the Terms — Continue" button at bottom (forest-green, full-width on mobile)
       - No "Back" button on this step

    2. **Create `src/components/ClientPersonalStep.tsx` (Step 2):**

       Client fills personal information using react-hook-form + zod validation.

       **Props:** `onNext: () => void`, `onBack: () => void`

       **Zod schema (`personalInfoSchema`):**
       - `fullName`: string, min 2, message "Please enter your full legal name as it appears on your ID."
       - `dob`: string, min 1, message "Date of birth is required."
       - `dlNumber`: string, min 1, message "Driver's license number is required."
       - `dlExp`: string, min 1, message "License expiration date is required."
       - `address`: string, min 5, message "Please enter your full street address."
       - `cityStateZip`: string, min 5, message "Please enter your city, state, and ZIP code."
       - `phonePrimary`: string, regex for 10+ digit phone, message "Please enter a valid phone number."
       - `phoneSecondary`: string, optional (allow empty)
       - `email`: string, email format, message "Please enter a valid email address."

       **Form behavior:**
       - Use `useForm` with `zodResolver(personalInfoSchema)`
       - Pre-fill `defaultValues` from `useClientSignStore` (persisted data)
       - On valid submit: update client sign store with all field values, call `onNext()`
       - On invalid: show inline error messages below each field (red text, professional wording)
       - Field validation on submit (not on blur — per Claude's discretion, this is cleaner for multi-step)

       **Layout (mobile-first per SIGN-05):**
       - Single column on mobile, two columns on md+
       - Each field uses a styled input with label above (NOT the existing InputLine component — create cleaner client-facing inputs with better mobile touch targets)
       - Input styling: rounded borders, generous padding (py-3 px-4), focus ring in forest-green, text-base for 16px (prevents iOS zoom on focus)
       - Professional guidance labels: "Full Legal Name", "Date of Birth", etc.
       - "Continue" button (forest-green, full-width on mobile) and "Back" text button

    3. **Create `src/components/ClientEmploymentStep.tsx` (Step 3):**

       Client fills employment information.

       **Props:** `onNext: () => void`, `onBack: () => void`

       **Zod schema (`employmentSchema`):**
       - `employerName`: string, min 2, message "Please enter your employer's name."
       - `employerPhone`: string, regex for 10+ digit phone, message "Please enter your employer's phone number."
       - `monthlyIncome`: string, min 1, message "Please enter your approximate monthly income."

       **Same form behavior and styling pattern as ClientPersonalStep.** Pre-fill from store, validate on submit, update store on success.

       **Layout:** Single column layout (only 3 fields). Heading: "Employment Information". Subtext: "This helps us verify your rental eligibility."

    4. **Create `src/components/ClientEmergencyStep.tsx` (Step 4):**

       Client fills emergency contact information.

       **Props:** `onNext: () => void`, `onBack: () => void`

       **Zod schema (`emergencyContactSchema`):**
       - `emergencyName`: string, min 2, message "Please enter your emergency contact's full name."
       - `emergencyPhone`: string, regex for 10+ digit phone, message "Please enter a valid phone number for your emergency contact."
       - `emergencyRelation`: string, min 1, message "Please specify your relationship to your emergency contact."

       **Same form behavior and styling pattern.** Pre-fill from store, validate on submit, update store on success.

       **Layout:** Single column. Heading: "Emergency Contact". Subtext: "Please provide someone we can contact in case of an emergency."

    **Common patterns across all form steps:**
    - Use `useClientSignStore` to read/write client data
    - Use the same input styling: `className="w-full rounded-lg border border-gray-300 px-4 py-3 text-base focus:outline-none focus:ring-2 focus:ring-forest-green focus:border-forest-green"` (text-base = 16px prevents iOS zoom)
    - Error messages: `<p className="text-alert-red text-sm mt-1">{error.message}</p>`
    - Navigation buttons at bottom: Back (text button, left) and Continue (forest-green solid button, right) in a flex row
    - Heading style: `text-xl font-bold text-forest-green mb-2`
    - Subtext style: `text-gray-600 text-sm mb-6`
    - All text uses professional + warm tone per user decision
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` passes
    - Each step component renders without errors
    - Zod schemas reject invalid input and produce user-friendly error messages
    - Form fields use text-base (16px) to prevent iOS zoom
  </verify>
  <done>Four client-facing wizard steps render with branded styling, RHF+zod validation, professional error messages, and mobile-optimized input sizing. Data persists to the client sign store on each step advance.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npx tsc --noEmit` passes
- /sign/:token loads the wizard with progress bar (when given a valid token)
- Invalid token redirects to /sign/expired
- Step 1 displays agreement terms as document text
- Steps 2-4 validate input with professional error messages
- Client data persists across browser refresh
</verification>

<success_criteria>
- Client opens /sign/:token and sees a branded wizard with Triple J logo and progress bar
- Step 1 shows admin-filled vehicle, rate, term, fee details as formatted document text (not inputs)
- Steps 2-4 capture personal info, employment, and emergency contact with validation
- Invalid/expired tokens redirect to the ExpiredPage
- Opening the link creates a 'viewed' audit log entry
- All form data persists in localStorage and survives browser refresh
- Inputs are 16px+ to prevent iOS auto-zoom
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-shareable-links-client-signing/02-03-SUMMARY.md`
</output>
