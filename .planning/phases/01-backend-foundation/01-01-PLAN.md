---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - index.html
  - tsconfig.json
  - src/main.css
  - src/App.tsx
  - src/index.tsx
  - src/types.ts
  - src/components/InputLine.tsx
  - src/components/Section.tsx
  - src/components/InitialsBox.tsx
  - src/components/AcknowledgmentBox.tsx
  - src/components/SignaturePad.tsx
autonomous: true

must_haves:
  truths:
    - "Dev server starts without errors after restructure"
    - "All existing components render with correct brand colors (forest green, gold, alert red)"
    - "Tailwind v4 build-time CSS replaces CDN play mode"
  artifacts:
    - path: "src/main.css"
      provides: "Tailwind v4 entry point with @theme custom colors"
      contains: "@import \"tailwindcss\""
    - path: "vite.config.ts"
      provides: "Vite config with @tailwindcss/vite plugin"
      contains: "tailwindcss"
    - path: "src/App.tsx"
      provides: "Existing app relocated to src/"
    - path: "src/types.ts"
      provides: "AgreementData type definition"
  key_links:
    - from: "index.html"
      to: "src/index.tsx"
      via: "script type=module src"
      pattern: "src/index\\.tsx"
    - from: "src/main.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: "@import.*tailwindcss"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugin array"
      pattern: "tailwindcss\\(\\)"
---

<objective>
Restructure the project into a standard src/ directory layout, install all Phase 1 dependencies, and migrate Tailwind CSS from CDN play mode to build-time @tailwindcss/vite v4.

Purpose: Every subsequent plan depends on the src/ directory structure, proper imports, and build-time Tailwind. This is the foundation that unblocks all other work.
Output: A running dev server with all existing components rendering correctly using Tailwind v4 build-time CSS.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md

Key existing files to relocate:
@App.tsx
@index.tsx
@index.html
@types.ts
@vite.config.ts
@tsconfig.json
@package.json
@components/InputLine.tsx
@components/Section.tsx
@components/InitialsBox.tsx
@components/AcknowledgmentBox.tsx
@components/SignaturePad.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create src/ directory structure</name>
  <files>package.json, tsconfig.json</files>
  <action>
  1. Install all Phase 1 dependencies:
     ```bash
     npm install react-router @supabase/supabase-js zustand
     npm install tailwindcss @tailwindcss/vite
     npm install -D supabase
     ```

  2. Create the src/ directory structure:
     ```
     src/
       components/    (move existing components here)
       pages/         (empty, for Plan 03)
       lib/           (empty, for Plan 02/03)
       stores/        (empty, for Plan 04)
     ```

  3. Move existing source files into src/:
     - `App.tsx` -> `src/App.tsx`
     - `index.tsx` -> `src/index.tsx`
     - `types.ts` -> `src/types.ts`
     - `components/*.tsx` -> `src/components/*.tsx`

  4. Update `tsconfig.json`:
     - Change the path alias from `"./*"` to `"./src/*"` so `@/` points to `src/`
     - Verify `"jsx": "react-jsx"` is set (already is)

  5. Update import paths in all moved files:
     - `src/index.tsx`: change `'./App'` to `'./App'` (same relative, no change needed since both moved)
     - `src/App.tsx`: change `'./components/Section'` etc. to `'./components/Section'` (same relative)
     - `src/App.tsx`: change `'./types'` to `'./types'` (same relative)
     - Verify all component cross-imports still work (AcknowledgmentBox imports InitialsBox)

  NOTE: Do NOT remove the CDN script tag from index.html yet -- that happens in Task 2.
  </action>
  <verify>
  Run `npx tsc --noEmit` from project root. Zero type errors.
  Verify `src/` directory has: App.tsx, index.tsx, types.ts, components/ (5 files), pages/, lib/, stores/
  </verify>
  <done>All source files live in src/. TypeScript compiles without errors. Empty directories ready for future plans.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Tailwind CSS from CDN to @tailwindcss/vite v4 build</name>
  <files>vite.config.ts, src/main.css, index.html, src/components/InputLine.tsx, src/components/Section.tsx, src/components/InitialsBox.tsx, src/components/AcknowledgmentBox.tsx, src/components/SignaturePad.tsx, src/App.tsx</files>
  <action>
  1. Create `src/main.css` with Tailwind v4 entry point and custom theme:
     ```css
     @import "tailwindcss";

     @theme {
       --color-forest-green: #1A472A;
       --color-gold: #C5A059;
       --color-alert-red: #8B1A1A;
       --color-charcoal: #2C2C2C;
       --color-light-gray: #F5F5F5;

       --font-sans: 'Inter', sans-serif;
       --font-serif: 'Lora', serif;
     }
     ```
     Then add the non-Tailwind CSS styles that were in index.html (legal-input, print styles, force-desktop styles, page-break, avoid-break, signature-pad styles) BELOW the @theme block.

  2. Update `vite.config.ts`:
     - Add `import tailwindcss from '@tailwindcss/vite'`
     - Add `tailwindcss()` to the plugins array (before or after react())
     - Remove the `define` block referencing GEMINI_API_KEY (not used)
     - Update the resolve alias to point `'@'` at `path.resolve(__dirname, 'src')`
     - Keep server config (port 3000, host 0.0.0.0)

  3. Update `index.html`:
     - REMOVE the CDN script tag: `<script src="https://cdn.tailwindcss.com"></script>`
     - REMOVE the html2pdf CDN script tag (not used in Phase 1, will be replaced by @react-pdf/renderer in Phase 3)
     - REMOVE the importmap script block (deps now come from node_modules via Vite)
     - REMOVE the inline `tailwind.config = { ... }` script block
     - REMOVE the inline `<style>` block (moved to src/main.css)
     - KEEP the Google Fonts link tags
     - Update the module script src from `/index.tsx` to `/src/index.tsx`
     - REMOVE the `<link rel="stylesheet" href="/index.css">` tag (replaced by import in index.tsx)

  4. Update `src/index.tsx`:
     - Add `import './main.css'` at the top (before React imports)

  5. Global find-and-replace for Tailwind v4 custom color class names across ALL component files and App.tsx:
     - `text-forestGreen` -> `text-forest-green`
     - `bg-forestGreen` -> `bg-forest-green`
     - `border-forestGreen` -> `border-forest-green`
     - `hover:bg-forestGreen` -> `hover:bg-forest-green` (check for any)
     - `text-alertRed` -> `text-alert-red`
     - `border-alertRed` -> `border-alert-red`
     - `hover:bg-alertRed` -> `hover:bg-alert-red`
     - `bg-lightGray` -> `bg-light-gray`
     - `text-charcoal` -> `text-charcoal` (single word, no change needed)
     - `border-charcoal` -> `border-charcoal` (single word, no change needed)
     - `text-gold` -> `text-gold` (single word, no change needed)
     - `border-gold` -> `border-gold` (single word, no change needed)
     - `bg-gold` -> `bg-gold` (single word, no change needed)
     - `ring-forestGreen` -> `ring-forest-green`
     - `focus:ring-forestGreen` -> `focus:ring-forest-green`

     Files to update (check all):
     - `src/App.tsx` (has forestGreen, alertRed, lightGray, gold, charcoal references)
     - `src/components/InputLine.tsx` (has forestGreen)
     - `src/components/Section.tsx` (has forestGreen, alertRed, gold)
     - `src/components/InitialsBox.tsx` (has forestGreen, charcoal)
     - `src/components/AcknowledgmentBox.tsx` (has lightGray, forestGreen)
     - `src/components/SignaturePad.tsx` (has forestGreen, alertRed, charcoal)

     Also update the `force-desktop` CSS rules in main.css -- these reference `md\:` prefixed classes which are fine as-is (they target Tailwind utility classes, not custom colors).

  6. Verify the body class in index.html: `text-charcoal font-serif` -- charcoal is one word so stays unchanged. Good.

  IMPORTANT: Do NOT use `@tailwind base; @tailwind components; @tailwind utilities;` -- that is Tailwind v3 syntax. Use `@import "tailwindcss"` for v4.
  IMPORTANT: Do NOT create a `tailwind.config.js` file -- Tailwind v4 uses CSS-first @theme configuration.
  </action>
  <verify>
  1. Run `npm run dev` -- dev server starts without errors
  2. Open http://localhost:3000 in a browser
  3. Verify the page renders with correct colors:
     - Header text "TRIPLE J AUTO INVESTMENT LLC" should be forest green (#1A472A)
     - "Vehicle Rental Agreement" heading should be forest green
     - Border under header should be gold (#C5A059)
     - Late payment warning text should be alert red (#8B1A1A)
  4. Run `npm run build` -- build completes without errors
  5. Confirm NO CDN script tags remain in index.html
  </verify>
  <done>Tailwind v4 build-time CSS is active. All custom colors render correctly. CDN removed. Dev server and production build both succeed.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npm run build` completes without errors
3. `npx tsc --noEmit` reports zero type errors
4. All 5 component files exist in `src/components/`
5. `src/main.css` contains `@import "tailwindcss"` and `@theme` block
6. `index.html` has no CDN script tags
7. All custom color classes use kebab-case (forest-green, alert-red, light-gray)
</verification>

<success_criteria>
- Dev server runs and renders the full agreement form with correct brand colors
- Zero CDN dependencies (tailwindcss, html2pdf, importmap all removed)
- All source files in src/ directory
- TypeScript compiles clean
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-01-SUMMARY.md`
</output>
